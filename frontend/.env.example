# -----------------------------------------------------------------------------
# Falsora Frontend Environment Variables
# -----------------------------------------------------------------------------

# Next.js Port (Default: 3000)
PORT=3000

# Core API Connection
# The frontend uses this to make requests to the Express backend
NEXT_PUBLIC_API_URL="http://localhost:4000/api"
